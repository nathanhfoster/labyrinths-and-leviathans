/**
 * Default initializer for a reducer
 * @param {ReducerState} stateOrProps - State or props
 * @returns {ReducerState}
 */
const defaultInitializer = (stateOrProps) => stateOrProps;

export default defaultInitializer;
